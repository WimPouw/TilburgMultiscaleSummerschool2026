# Mediapiping — full-body tracking + masking/blurring/tracing
# CPU-only. Works on Python 3.9–3.12 (NOT 3.13 yet — mediapipe has no 3.13 wheels).
mediapipe==0.10.10     # Holistic tracking + body segmentation (legacy solutions API; verified working)
opencv-python>=4.8     # video read/write and image operations (verified with OpenCV 4.x and 5.0)
numpy>=1.24
pandas>=2.0
jupyter
ipykernel
ipython

# NOTE: the gesture detector `envisionhgdetector` is a SEPARATE tool with its own
# environment. It pins mediapipe==0.10.7, which conflicts with the 0.10.10 the
# masking notebook needs, so it is intentionally NOT installed here.
